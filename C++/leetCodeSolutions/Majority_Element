// Sorting This vector always shows that majority element will always be in the middle.
class Solution {
public:
    int majorityElement(vector<int>& nums) {
       sort(nums.begin(), nums.end());
       return nums[nums.size()/2];
    }
};

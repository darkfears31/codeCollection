class Solution {
public:
    int titleToNumber(string columnTitle) {
        int result{0};
        for(char c:columnTitle){
            int a = c-'A'+1; // subtract ASCII value of 'A' to ASCII value of char c and plus one to get position.
            result=result*26+a;
        }
        return result;
    }
};
